apiVersion: apps/v1
kind: Deployment
metadata:
  name: kuber0demo-api
  labels:
    app: kuber0demo-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: kuber0demo-api
  template:
    metadata:
      labels:
        app: kuber0demo-api
    spec:
      containers:
        - name: kuber0demo-api
          image: codenzen117/kuber0demo-api:latest
          ports:
            - containerPort: 3000
          env:
            - name: NODE_ENV
              value: production
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "256m"
          readinessProbe:
            httpGet:
              path: /readyz
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 20
